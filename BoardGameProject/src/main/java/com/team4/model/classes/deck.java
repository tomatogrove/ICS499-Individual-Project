package com.backend.classes;


public class deck {
	private Card[] cards;
	private int cardsInDeck;
	
	public deck()
	{ 
		cards = new Card[108]
	}
	public void reset()
			card.color[]	 colors = card.color.values();
			cardInDeck = 0;
			
			for(int i = 0; i < color.length-1; i++)
			{
				card.color colors = color[i];
				
				card[cardInDeck++] = new card(colors, card.Vakue.getValue(0));
				
				for(int j = 1; j < 10; j++)
				{
					card[cardInDeck++] = new card(colors, card.Value.getCalue(j));
					card[cardInDeck++] = new card(colors, card.Value.getCalue(j));
				}
			}
}
